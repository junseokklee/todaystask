{
  "name": "todaystask",
  "private": true,
  "workspaces": [
    "clock-app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace clock-app",
    "build": "npm run build --workspace clock-app",
    "start": "npm run start --workspace clock-app",
    "lint": "npm run lint --workspace clock-app"
  }
}
